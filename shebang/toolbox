#!/usr/bin/env bash
echo -e 'archive\t\t\t\tzip a folder with maximum compression'
echo -e 'clean\t\t\t\tUninstall unused dependencies'
echo -e 'define\t\t\t\tInteractive wrapper around Wordnet. Dictionary/Thesaurus/etc.'
echo -e 'dl\t\t\t\tyt-dlp+chat_downloader+transcribe'
echo -e 'duration\t\t\tGet the duration of a file'
echo -e 'edchat\t\t\t\tWatch Elite Dangerous logs for incoming chat messages and print them'
echo -e 'fps\t\t\t\tGet the fps of a video'
echo -e 'gif\t\t\t\tTurn a video into a gif'
echo -e 'github_latest_released_version\tGets the latest released version name of a github repo'
echo -e 'is_command\t\t\tHas an exit code of 0 or 1 depending on if a command exists'
echo -e 'isolate\t\t\t\tGo through all files in this folder, group together ones that are the same before the first non-starting . into their own folder'
echo -e 'llc\t\t\t\tLive Line Count. Pipe output into it and count the lines as they appear. llc --help for more options'
echo -e 'maintenance\t\t\tUpdate mirrors, system, vacuum old log files, remove cache. Run this every month or two.'
echo -e 'mcchat\t\t\t\tExtract all chat messages from minecraft log files in the current directory'
echo -e 'memetext\t\t\tOverlay text on top of media'
echo -e 'menu_scripts\t\t\tMeant to be run via a keybind'
echo -e 'moon\t\t\t\tGet the current phase of the moon'
echo -e 'my_local_ip\t\t\tPrint local ip'
echo -e 'my_public_ip\t\t\tPrint public ip'
echo -e 'my_ip\t\t\t\tPrint local & public ip'
echo -e 'notification_history\t\tDunst notification history'
echo -e 'obs_replay_notif\t\tGet a notification whenever a new file appears in $HOME/Videos/'
echo -e 'package_count\t\t\tCount how many packages you have from different package managers'
echo -e 'resend_notification\t\tResend the last n notifications from dunst, with each waiting to make sure you see them'
echo -e 'resolution\t\t\tGet the resolution of a video'
echo -e 'sa\t\t\t\tSearch All. fzf the contents of all available files and open nvim on the selected line'
echo -e 'sortlen\t\t\t\tSort stdin lines from shortest to longest'
echo -e 'sortlenrev\t\t\tSort stdin lines from longest to shortest'
echo -e 'store\t\t\t\tzip a folder without any compression'
echo -e 'termsize\t\t\tMeasure how many characters wide/tall your terminal is'
echo -e 'toolbox\t\t\t\tThis. Lists everything in your toolbox.'
echo -e "transcribe\t\t\tCreate a trascript for a .wav file. If file isn't a .wav file, one will be created from the provided file."
echo -e 'tstyle\t\t\t\tEasily style terminal output without having to remember ansi escape codes or tput codes. tstyle --help for more information'
echo -e 'update\t\t\t\tUpdate Arch packages, Rust, Cargo packages, and Flatpak packages'
echo -e 'update_balatro_mods\t\tUpdate lovely injector, steamodded, and Balatro multiplayer mod'
echo -e 'update_mirrors\t\t\tRerank pacman mirrors'
echo -e 'weather\t\t\t\tGet the weather'
echo -e 'whisper_transcript_to_srt\tShould only be used by transcribe'
echo -e 'without_last_extension\t\tRemove the last extension of whatever is piped into it'
